Django>=5.0
openpyxl
pyxlsb
python-dotenv
gunicorn
whitenoise
google-cloud-bigquery
google-auth
# NOTE: the fast columnar download (BigQuery Storage API + Arrow) is intentionally
# NOT installed. The target project's service account doesn't have the BigQuery
# Storage Read API enabled, and in that case `to_arrow(create_bqstorage_client=True)`
# can return an EMPTY table without raising — silently dropping all rows. Staying
# on the REST download keeps results correct. The invoice enrichment is kept fast
# instead by filtering to the invoice's carrier and only deriving products for the
# invoice's AWBs. If you later enable the Storage Read API for the SA, you can add:
google-cloud-bigquery-storage
#   pyarrow
